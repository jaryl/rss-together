Hey there,

<%= @sender.display_name %> has invited to their group.

Join the group with this link:
<%= rss_together.join_url(token: @invitation.token) %>

You'll have to create an RSSTogether account if you don't already have one.
